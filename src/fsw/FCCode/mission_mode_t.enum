#ifndef mission_mode_t_enum_
#define mission_mode_t_enum_

// pan.mode
enum class mission_mode_t {
    warmup,
    initialization,
    accel_calibration,
    standby,
    detumble,
    bellyflop,
    landed,
    starhopper1, // starhopper1-3 coorespond to different setpoints in the hop
    starhopper2,
    starhopper3,
};

#endif
